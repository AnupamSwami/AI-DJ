function setup() {
    canvas = createCanvas(400,300) ;
    canvas.position(500,200) ;
    video = createCapture(VIDEO) ;
    video.size(400,300) ;
    video.position(500,200) ;
}

function draw() {
    background('white') ;
}